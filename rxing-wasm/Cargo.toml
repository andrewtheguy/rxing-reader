[package]
name = "rxing-wasm"
version = "0.1.0"
edition = "2021"
description = "WASM QR reader wrapper around the trimmed rxing crate."
license = "Apache-2.0"
repository = "https://github.com/andrewtheguy/rxing-reader"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3"
rxing = { path = "../rxing-vendored" }
